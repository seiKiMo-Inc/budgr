{
    "name": "@repo/ui",
    "version": "0.0.0",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsup",
        "dev": "tsup --wathc",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@repo/typescript-config": "*",
        "tsup": "^8.4.0",
        "typescript": "^5.8.3"
    }
}